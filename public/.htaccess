RewriteEngine On
RewriteBase /

# Serve existing files (including Vite assets) directly.
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Let the React SPA resolve client-side routes on direct visits.
RewriteRule ^ index.html [L]
